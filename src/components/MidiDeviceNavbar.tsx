import { MidiDeviceNavbarDesktop } from "./MidiDeviceNavbarDesktop";

// Kept for backward compatibility - forwards to desktop version
export function MidiDeviceNavbar() {
  return <MidiDeviceNavbarDesktop />;
}
